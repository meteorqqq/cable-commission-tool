[server]
headless = true

[client]
toolbarMode = "minimal"
showErrorDetails = false

[browser]
gatherUsageStats = false

[global]
# 跨页面保留筛选时会主动用 session_state 写回控件值，与控件自带默认值并存，
# Streamlit 会就此记录一次告警；这是本应用的预期行为，关闭以保持日志干净。
disableWidgetStateDuplicationWarning = true

[theme]
base = "light"
